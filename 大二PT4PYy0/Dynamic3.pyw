from pt4 import *
# An integer D and the top A1 of a nonempty stack are given.
# Push a component with the value D onto the stack and output a reference A2 to a new top of the stack.
def solve():
    task("Dynamic3")
    d,p1=get2()
    p2=Node(d,p1)
    put(p2)








start(solve)
